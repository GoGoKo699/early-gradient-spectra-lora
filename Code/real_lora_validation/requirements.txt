# Dependencies for the corrected real GPT-2 publication rerun.
# Install the platform-specific PyTorch 2.9.1 CUDA/ROCm build first.
# The core model stack is pinned to the environment recorded by the project;
# the final runner records the complete resolved environment with `pip freeze`.
numpy==1.26.4
transformers==5.12.0
datasets==5.0.0
accelerate==1.14.0
peft==0.19.1
safetensors==0.8.0
tokenizers==0.22.2
pyarrow
pandas
scikit-learn
evaluate
tqdm
# Direct dependencies of the publication input/rerun helpers.
huggingface_hub==1.16.1
pytest==9.0.2
tabulate==0.9.0
