# Released synthetic-transformer result artifacts

The compact paper-facing synthetic-transformer summaries are stored in:

```text
paper_tables/
```

These are superseded derived tables from the earlier manuscript. Their readable summaries are now Markdown; original versions remain in Git history. Current compact evidence is under `evidence/transformer/` at the repository root. Full local run directories are separate artifacts.
