# Released synthetic-transformer result artifacts

The compact paper-facing synthetic-transformer summaries are stored in:

```text
paper_tables/
```

These files are copied from the Overleaf paper tables and are included so the code package has an explicit released-result location. Full local run directories are intentionally excluded from the clean release package.
